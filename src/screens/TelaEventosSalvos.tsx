import { Text, View } from 'react-native';
import globalStyles from '../styles/globalStyles';

export default function TelaEventosSalvos() {
  return (
    <View style={globalStyles.tela}>
      <Text>Eventos salvos</Text>
    </View>
  );
}
